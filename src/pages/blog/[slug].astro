---
/**
 * Individual blog post page — NOT implemented in MVP.
 *
 * To implement:
 * 1. Import getStaticPaths and getCollection from 'astro:content'
 * 2. Define getStaticPaths() to return all blog post slugs
 * 3. Render post content with <Content /> component
 *
 * See docs/ARCHITECTURE.md — "Future: Blog / Case Studies" section.
 */

// Placeholder: generates no static pages until content is added.
export function getStaticPaths() {
  return [];
}
---
